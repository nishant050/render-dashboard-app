{
  "name": "render-dashboard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "render:build": "npm ci && python -m pip install --upgrade pip && pip install -r requirements.txt && pip install -r apps/DietPlan/requirements.txt && (command -v ffmpeg >/dev/null 2>&1 || (apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/*)) && ffmpeg -version",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "adm-zip": "^0.5.16",
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "express": "^5.1.0",
    "groq-sdk": "^0.27.0",
    "http-proxy-middleware": "^3.0.5",
    "mongoose": "^9.3.0",
    "multer": "^2.0.2",
    "puppeteer": "^24.37.3"
  }
}
